# 清理与取舍报告

## 这次实际清理的范围

清理发生在新建的公开分享包中。原始工作区和 `D:\龙女仆同人流` 归档没有删除，避免损失可追溯记录。

公开包没有复制：

- 所有 `.bak`、`before_*` 与旧 CMD/VBS 备份；
- `runs_v2` 中的个人运行清单；
- `__pycache__`、`.pyc`、临时文件和日志；
- 70 个逐图 API JSON；改为 7 个代表流 + 70 条提示词 MD + 参数 CSV；
- `.safetensors/.ckpt/.pth/.index/.bin/.gguf`；
- 第三方模型、角色卡、参考音频和生成 CG；
- 旧的成人 img2img 测试工作流。

## 已确认的完全重复

以下三个工作区文件与 `D:\龙女仆同人流\00_龙女仆AI创作总入口\批量提示词输入器` 对应文件 SHA-256 相同：

- `batch_prompt_tool_v2.py`
- `test_batch_prompt_tool_v2.py`
- `README_批量输入说明.md`

公开包只放一份。GUI 仍允许手动选择包内 `workflows_ui` 和 `04_ComfyUI工作流` 下的 JSON；源码中保留的本机默认目录只是作者环境兼容项。

## 被排除的连续性工作流

`workflow_img2img_continuity_20260822` 的三份旧 JSON 含成人测试正面词；所谓 SFW clean 版本仍在负面词和文件名中保留大量成人词，同时来源路径和命名不适合作为公开新手模板。因此本包不附带这一组文件。

如果以后公开连续镜头示例，应从空白基础流重新创建通用 SFW 版，而不是在旧成人流上继续删词。

## 为什么没有直接删除原始备份

`runs_v2` 可用于追溯历史 seed、任务和输出；`.bak` 也可能是唯一的版本回退点。它们对公开包是冗余，对作者归档未必是垃圾。若要清理原始目录，应先输出精确候选清单、大小、最后修改时间与哈希，再移动到回收站；不要清空回收站，验收后再决定。
